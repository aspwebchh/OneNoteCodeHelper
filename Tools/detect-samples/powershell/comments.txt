# Backup the folder; select files from source where size > 0
$source = "C:\Data"
$target = "D:\Backup"
if (-not (Test-Path $target)) {
    New-Item -ItemType Directory -Path $target | Out-Null
}
Copy-Item -Path "$source\*" -Destination $target -Recurse -Force
Write-Host "Done"
